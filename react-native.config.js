/**
 * react-native CLI config.
 * `assets` lists folders whose font/asset files get copied into the native
 * projects when you run `npx react-native-asset` (script: `npm run fonts`).
 */
module.exports = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./assets/fonts'],
};
